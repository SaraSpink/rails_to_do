<h1>New task for <%= @list.name %></h1>

<%= render('layouts/errors', :object => @task) %>

<%= form_for [@list, @task] do |f| %>
  <%= f.label :description %>
  <%= f.text_field :description %>
  <br />

  <p><%= f.submit %></p>



<% end %>
